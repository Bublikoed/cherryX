# Template HTML

Готовий шаблон для звичайної верстки з HTML, CSS та JavaScript.

## 🚀 Швидкий старт

### Встановлення залежностей

```bash
npm install
```

### Запуск сервера розробки

```bash
npm run dev
```

Сервер автоматично відкриється в браузері на `http://localhost:3000`

### Збірка для продакшну

```bash
npm run build
```

Готові файли будуть в папці `dist/`

## 📁 Структура проєкту

```
templateHTML/
├── src/                    # Вихідні файли
│   ├── css/               # CSS файли
│   │   └── main.css       # Основний CSS файл
│   ├── js/                # JavaScript файли
│   │   └── main.js        # Основний JS файл
│   ├── images/            # Зображення
│   ├── icons/             # Іконки
│   └── fonts/             # Шрифти
├── dist/                  # Зібрані файли (створюється автоматично)
├── index.html             # Головна HTML сторінка
├── package.json           # Залежності та скрипти
├── vite.config.js         # Конфігурація Vite
├── .eslintrc.js           # Налаштування ESLint
├── .prettierrc            # Налаштування Prettier
└── README.md              # Цей файл
```

## 🛠 Доступні команди

| Команда | Опис |
|---------|------|
| `npm run dev` | Запуск сервера розробки з live reload |
| `npm run build` | Збірка проєкту для продакшну |
| `npm run preview` | Попередній перегляд зібраного проєкту |
| `npm run lint` | Перевірка коду ESLint |
| `npm run lint:fix` | Автоматичне виправлення помилок ESLint |
| `npm run format` | Форматування коду Prettier |
| `npm run format:check` | Перевірка форматування коду |

## ⚙️ Налаштування

### Vite

Конфігурація знаходиться в `vite.config.js`:
- Сервер розробки на порту 3000
- Автоматичне відкриття браузера
- Оптимізована збірка з мінімізацією
- Організація вихідних файлів по папках

### ESLint

Налаштування в `.eslintrc.js`:
- Перевірка JavaScript коду
- Інтеграція з Prettier
- Правила для браузерного середовища

### Prettier

Налаштування в `.prettierrc`:
- Автоматичне форматування коду
- Консистентний стиль коду
- Підтримка HTML, CSS, JS

### VS Code

Налаштування в `.vscode/settings.json`:
- Автоматичне форматування при збереженні
- Інтеграція з ESLint та Prettier
- Рекомендовані розширення

## 📝 Робота з файлами

### HTML
- Головний файл: `index.html`
- Підключайте CSS та JS файли з папки `src/`

### CSS
- Основний файл: `src/css/main.css`
- Додавайте нові CSS файли в папку `src/css/`
- Імпортуйте їх в `main.css` або підключайте в HTML

### JavaScript
- Основний файл: `src/js/main.js`
- Додавайте нові JS файли в папку `src/js/`
- Підключайте їх в HTML або імпортуйте в `main.js`

### Ресурси
- Зображення: `src/images/`
- Іконки: `src/icons/`
- Шрифти: `src/fonts/`

## 🔧 Розширення функціональності

### Додавання нових сторінок
1. Створіть новий HTML файл в корені проєкту
2. Підключіть CSS та JS файли
3. Додайте посилання в навігацію

### Додавання нових стилів
1. Створіть новий CSS файл в `src/css/`
2. Імпортуйте його в `main.css` або підключіть в HTML

### Додавання JavaScript модулів
1. Створіть новий JS файл в `src/js/`
2. Імпортуйте його в `main.js` або підключіть в HTML

## 🚀 Деплой

### Статичний хостинг
Після виконання `npm run build`:
1. Завантажте вміст папки `dist/` на ваш хостинг
2. Налаштуйте веб-сервер для обслуговування статичних файлів

### GitHub Pages
1. Підключіть репозиторій до GitHub
2. Увімкніть GitHub Pages в налаштуваннях репозиторію
3. Вкажіть папку `dist/` як джерело

## 📚 Корисні посилання

- [Vite Documentation](https://vitejs.dev/)
- [ESLint Documentation](https://eslint.org/)
- [Prettier Documentation](https://prettier.io/)
- [HTML Reference](https://developer.mozilla.org/en-US/docs/Web/HTML)
- [CSS Reference](https://developer.mozilla.org/en-US/docs/Web/CSS)
- [JavaScript Reference](https://developer.mozilla.org/en-US/docs/Web/JavaScript)

## 📄 Ліцензія

MIT License - використовуйте вільно для ваших проєктів.
